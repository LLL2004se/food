import React from "react";

/**
 * This component is no longer needed since admin dark mode is handled separately
 * at the admin layout level. Kept for backward compatibility.
 */
export default function DarkMode() {
  return null;
}
